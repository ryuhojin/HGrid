{
  "name": "@hgrid/grid-vue",
  "version": "0.0.0",
  "description": "Vue adapter for HGrid",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@hgrid/grid-core": "workspace:*"
  },
  "peerDependencies": {
    "vue": "^3.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet\""
  }
}
